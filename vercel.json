{
    "version": 2,
    "builds": [
      { "src": "/backend/**", "use": "@vercel/node" },
      { "src": "/frontend/**", "use": "@vercel/static" }
    ],
    "routes": [
      {
        "src": "/api/(.*)",
        "dest": "backend/api/$1",
        "methods": ["GET", "POST", "PUT", "PATCH", "DELETE"]
      },
      {
        "src": "/(.*)",
        "dest": "frontend/$1"
      }
    ]
  }
  